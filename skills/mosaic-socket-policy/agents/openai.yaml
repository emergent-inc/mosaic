interface:
  display_name: "mosaic Socket Policy"
  short_description: "Keep mosaic socket commands off hot main-thread paths and focus-safe."
  default_prompt: "Use this skill when adding or changing mosaic socket or CLI commands, telemetry handlers, focus/select/open/close/send-key behavior, or automation that could activate the app or steal focus."
