interface:
  display_name: "mosaic Release"
  short_description: "Prepare mosaic releases with version, changelog, guard, tag, and asset rules."
  default_prompt: "Use this skill when preparing a mosaic release, bumping versions, updating CHANGELOG.md, running the release pretag guard, tagging, or troubleshooting release assets."
