interface:
  display_name: "mosaic Localization"
  short_description: "Localize and audit every mosaic user-facing string change."
  default_prompt: "Use this skill whenever changing mosaic UI text, settings labels, menu items, shortcut metadata, schema/config text, docs, command/help output, alerts, tooltips, or web messages."
