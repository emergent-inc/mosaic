interface:
  display_name: "mosaic Dev Workflow"
  short_description: "Follow mosaic setup, Xcode, and tagged ExtensionKit development rules."
  default_prompt: "Use this skill when setting up mosaic, touching Xcode project files, working with tagged debug builds, or adding sidebar extensions that must coexist with other dev builds."
